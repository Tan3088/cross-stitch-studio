import type { FabricCount } from '../types'

/** Common Aida and evenweave fabric counts (stitches per inch). */
export const FABRIC_COUNTS: FabricCount[] = [11, 14, 16, 18, 20, 22, 25, 28, 32]

export const BORDER_MARGIN_PRESETS = [2, 3, 4] as const
